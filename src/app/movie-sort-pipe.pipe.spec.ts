import { MovieSortPipePipe } from './movie-sort-pipe.pipe';

describe('MovieSortPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MovieSortPipePipe();
    expect(pipe).toBeTruthy();
  });
});
